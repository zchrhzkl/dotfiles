# Generated by `chezmoi init` into ~/.config/chezmoi/chezmoi.toml.
# sourceDir is resolved at init time, so this repo works whether it lives in
# ~/.local/share/chezmoi (the README bootstrap) or anywhere else you cloned it.
sourceDir = {{ .chezmoi.sourceDir | quote }}

[data]
